{
  "name": "mizo-dub-studio",
  "version": "0.1.0",
  "description": "Windows-local desktop prototype for Mizo movie recap dubbing.",
  "main": "src/main/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main/main.js && node --check src/main/pipeline.js && node --check src/main/preload.js && node --check src/renderer/app.js && node --check scripts/smoke-test.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "devDependencies": {
    "electron": "^41.7.1"
  }
}
